import DancingLines from "react-dancing-lines";

export const DancingLines = () => {
  // const getSupportsTouch = () => (navigator.maxTouchPoints ? true : false);

  // can not be dark on SSR
  return <DancingLines />;
};
